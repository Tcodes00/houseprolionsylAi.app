# 🏠 House Price Prediction Deployment

A professional machine learning project that predicts house prices using real estate data, complete with web interface and analytics.

## 🚀 Quick Start Guide

### Prerequisites
- Python 3.8 or higher
- pip (Python package manager)

### Step 1: Installation

1. **Create project folder and add files:**
   - Create a new folder called `house_price_deployment`
   - Save all the provided files in this folder
   - Make sure `synthetic_house_prices.csv` is in the folder

2. **Install Python** (if not installed):
   - Download from [python.org](https://python.org)
   - During installation, CHECK "Add Python to PATH"

3. **Open Command Prompt/Terminal:**
   - Windows: Press `Win + R`, type `cmd`, press Enter
   - Mac: Press `Cmd + Space`, type `terminal`, press Enter

### Step 2: Setup Virtual Environment

```bash
# Navigate to your project folder
cd path/to/house_price_deployment

# Create virtual environment (recommended)
python -m venv venv

# Activate virtual environment
# On Windows:
venv\Scripts\activate
# On Mac/Linux:
source venv/bin/activate